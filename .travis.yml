sudo: required
services:
  - docker

before_install:
  - docker build -t fatihkelesdev/docker-react -f Dockerfile.dev .

script:
  - docker run fatihkelesdev/docker-react npm run test -- --coverage


